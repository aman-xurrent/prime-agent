---
name: phase-2
description: Phase 2 development — structures only, no logic. Use when the user asks for phase 2, data shapes first, structs-only work, scaffolding types, or when a feature should stop at type definitions before behavior is implemented.
paths: |
  Backend (Ruby DSL) - ./connector/lib/ipaas/connector/
  - Core structures: action.rb, trigger.rb, runbook.rb, connection.rb, schema.rb etc.
  - Type system: /types/ directory with type classes (string_type.rb, integer_type.rb, nested_type.rb, schema_field_type.rb, etc.)
  - DSL builders: /dsl/ directory
  - Schema generation: /schema/ directory (field.rb, structure_inferrer.rb, field_builder.rb etc.)

  Backend (Models/Platform) — ./platform/app/models/
  - Core entities: job.rb, solution.rb, runbook_record.rb, account.rb, user.rb, schedule.rb etc.
  - Job system: job_state_entry.rb, job_connector_task.rb etc.
  - Observability: log_entry.rb, audit_event.rb, error_log.rb, trigger_event.rb etc.
  - Concerns (shared behavior): /concerns/ directory

  Frontend (React/TypeScript) — ./platform/app/javascript/types/
  - modelTypes.ts — all TypeScript interface definitions for models
  - workPerformedTypes.ts — work-performed structures

  Shared (SDK/Public API) — ./connector-sdk/lib/ipaas/
  - Customer-facing SDK structures

  Job execution — ./connector/lib/ipaas/job/
  - Job state management and context structures

  Test case structures — ./connector/lib/ipaas/test_case/
  
---

# Phase 2: Structures Only
* change structures/models/interfaces files to have the desired goal for this change.
* the project SHOULD NOT be built at this point.  We are ONLY doing structural changes, by definition some things should break cuz behavior is intentionally not implemented in this phase.

## Phase 2 done criteria

- [ ] update to latest main before proceeding
- [ ] All new/changed types live in allowed locations above.
- [ ] No procedures, logic, or behavioral wiring were added.
- [ ] request/ticket is moved to review column and assigned to me.
- [ ] Phase 2 commit pushed and **PR created or updated immediately**.
- [ ] internal note is published saying **Phase 2: {pr link} {Created/Updated}**
